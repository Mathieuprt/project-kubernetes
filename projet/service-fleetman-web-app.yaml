apiVersion: v1
kind: Service
metadata:
  name: fleetman-web-app
  labels:
    app: fleetman
    tier: web-app
spec:
  type: NodePort
  selector:
    app: fleetman
    tier: web-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
